# 工商登記查詢系統 — 執行期相依套件
# 版本號依本機開發／測試當下實際使用版本鎖定（2026-08-14），部署 Render 時
# 會安裝此處指定版本，避免日後套件更新造成非預期的行為差異。
Flask==3.1.3
Werkzeug==3.1.8
requests==2.34.2
gunicorn==23.0.0
# 裁判書查詢（FJUD）功能新增：Playwright 用於自動化操作司法院裁判書查詢網站。
# 部署到 Render 時，Build Command 除了 pip install -r requirements.txt，
# 還必須額外執行 playwright install --with-deps chromium 安裝瀏覽器執行檔，
# 否則 Playwright 找不到瀏覽器會直接查詢失敗。
playwright==1.47.0
